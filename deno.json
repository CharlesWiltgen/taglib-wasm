{
  "lint": {
    "rules": {
      "exclude": [
        "no-explicit-any",
        "no-unused-vars",
        "require-await"
      ]
    },
    "exclude": ["lib/", "build/", "scripts/", "examples/", "tools/"]
  },
  "fmt": {
    "exclude": ["lib/", "build/"]
  }
}
